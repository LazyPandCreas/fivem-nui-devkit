# Copia in .env.local e scegli la modalita:
#   mock   -> dati finti, nessuna dipendenza (default se non impostato)
#   bridge -> Lua + DB veri via FXServer headless (vedi docs/EVENTI-FRONTEND-BACKEND.md)
VITE_NUI_MODE=mock
VITE_BRIDGE_URL=http://localhost:30125
